# Gate-0 workflow: versioning + deviation detection

name: Gate-0 Versioning + Deviation Detection

on:
  push:
    branches: [main, develop]
    paths:
      - 'backend/**'
      - 'data/**'
      - 'openspec/**'
      - 'scripts/gates/gate0_versioning_deviation.sh'
      - '.github/workflows/gate0-versioning-deviation.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'backend/**'
      - 'data/**'
      - 'openspec/**'
      - 'scripts/gates/gate0_versioning_deviation.sh'
      - '.github/workflows/gate0-versioning-deviation.yml'
  workflow_dispatch: {}

env:
  PYTHON_VERSION: '3.12'

jobs:
  gate0:
    name: Gate-0 (Versioning + Deviation)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install OpenSpec CLI
        run: |
          npm install -g @fission-ai/openspec@0.17.2
          openspec --version

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Gate-0 acceptance
        run: |
          bash scripts/gates/gate0_versioning_deviation.sh

      - name: Upload Gate-0 artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: gate0-versioning-deviation-artifacts
          path: artifacts/
          retention-days: 30


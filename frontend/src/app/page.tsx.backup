'use client';

import { useState } from "react";

type View = "now" | "timeline" | "archive";
type ActiveSheet = "none" | "dream" | "guidance" | "mood" | "timeline";

export default function Home() {
  const [view, setView] = useState<View>("now");
  const [activeSheet, setActiveSheet] = useState<ActiveSheet>("none");

  return (
    <div className="min-h-screen bg-[radial-gradient(circle_at_top,_var(--bg-top),_var(--bg-middle)_40%,_var(--bg-bottom)_100%)] text-[color:var(--text-primary)]">
      <div className="mx-auto flex min-h-screen w-full max-w-6xl">
        <aside className="hidden w-48 flex-col border-r border-[var(--card-border)] px-4 py-10 md:flex">
          <button
            type="button"
            className="flex items-center gap-3 text-left text-[color:var(--text-primary)]"
            onClick={() => setView("now")}
          >
            <div className="h-9 w-9 rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-amber))] glow breathing" />
            <div className="flex flex-col">
              <span className="text-xs font-medium tracking-[0.25em] text-[color:var(--text-tertiary)]">
                LUCIDSELF
              </span>
              <span className="text-sm text-[color:var(--text-secondary)]">
                灵性伴侣
              </span>
            </div>
          </button>
          <nav className="mt-10 flex flex-1 flex-col gap-2 text-xs text-[color:var(--text-secondary)]">
            <button
              type="button"
              onClick={() => setView("now")}
              className={`flex items-center gap-3 rounded-[var(--radius-md)] px-3 py-2 text-left transition-colors ${view === "now" ? "bg-[var(--card-bg)] text-[color:var(--text-primary)]" : "hover:bg-[var(--card-bg)]"}`}
            >
              <span className="h-1.5 w-1.5 rounded-full" style={{ background: view === "now" ? "var(--accent-gold)" : "transparent" }} />
              <span>Now 当下界</span>
            </button>
            <button
              type="button"
              onClick={() => setView("timeline")}
              className={`flex items-center gap-3 rounded-[var(--radius-md)] px-3 py-2 text-left transition-colors ${view === "timeline" ? "bg-[var(--card-bg)] text-[color:var(--text-primary)]" : "hover:bg-[var(--card-bg)]"}`}
            >
              <span className="h-1.5 w-1.5 rounded-full" style={{ background: view === "timeline" ? "var(--accent-gold)" : "transparent" }} />
              <span>Timeline 时间界</span>
            </button>
            <button
              type="button"
              onClick={() => setView("archive")}
              className={`flex items-center gap-3 rounded-[var(--radius-md)] px-3 py-2 text-left transition-colors ${view === "archive" ? "bg-[var(--card-bg)] text-[color:var(--text-primary)]" : "hover:bg-[var(--card-bg)]"}`}
            >
              <span className="h-1.5 w-1.5 rounded-full" style={{ background: view === "archive" ? "var(--accent-gold)" : "transparent" }} />
              <span>Archive 永恒界</span>
            </button>
          </nav>
        </aside>
        <div className="flex min-h-screen flex-1 flex-col px-6 py-6 md:px-10 md:py-10">
          <header className="mb-6 flex items-center justify-between md:hidden">
            <button
              type="button"
              className="flex items-center gap-2 text-left"
              onClick={() => setView("now")}
            >
              <div className="h-8 w-8 rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-amber))] glow breathing" />
              <div className="flex flex-col">
                <span className="text-xs font-medium tracking-[0.25em] text-[color:var(--text-tertiary)]">
                  LUCIDSELF
                </span>
                <span className="text-xs text-[color:var(--text-secondary)]">
                  内在成长实验空间
                </span>
              </div>
            </button>
          </header>
          <header className="mb-10 hidden items-center justify-between gap-4 md:flex">
            <div className="flex items-center gap-3">
              <div className="h-9 w-9 rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-amber))] glow breathing" />
              <div className="flex flex-col">
                <span className="text-xs font-medium tracking-[0.25em] text-[color:var(--text-tertiary)]">
                  LUCIDSELF
                </span>
                <span className="text-sm text-[color:var(--text-secondary)]">
                  Less UI, More Soul
                </span>
              </div>
            </div>
          </header>
          <main className="flex-1">
            {view === "now" && (
              <section className="rounded-[var(--radius-xl)] border border-[var(--card-border)] bg-[var(--card-bg)] p-6 shadow-[var(--card-shadow)] backdrop-blur-2xl md:p-10">
                <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                  Now · 当下界
                </p>
                <h1 className="mt-4 text-3xl md:text-4xl breathing">
                  你现在想做什么？
                </h1>
                <p className="mt-3 max-w-2xl text-sm text-[color:var(--text-secondary)] md:text-base">
                  不是刷完信息，而是完成一件小小的灵魂任务。选择一个动作开始今天的 LucidSelf。
                </p>
                <div className="mt-8 grid gap-4 md:grid-cols-2">
                  <button
                    type="button"
                    onClick={() => setActiveSheet("dream")}
                    className="group flex items-start gap-3 rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--card-bg)] px-5 py-4 text-left shadow-[var(--card-shadow)] transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[var(--card-hover-shadow)]"
                  >
                    <div className="mt-0.5 flex h-9 w-9 items-center justify-center rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-violet),_var(--accent-emerald))] text-xs font-semibold text-white">
                      梦
                    </div>
                    <div className="flex flex-col gap-1">
                      <span className="text-sm font-semibold text-[color:var(--text-primary)]">
                        记录梦境
                      </span>
                      <span className="text-xs text-[color:var(--text-secondary)]">
                        在醒来后的数分钟内捕捉梦的残影，未来由引擎帮你提炼象征与线索。
                      </span>
                    </div>
                  </button>
                  <button
                    type="button"
                    onClick={() => setActiveSheet("guidance")}
                    className="group flex items-start gap-3 rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--card-bg)] px-5 py-4 text-left shadow-[var(--card-shadow)] transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[var(--card-hover-shadow)]"
                  >
                    <div className="mt-0.5 flex h-9 w-9 items-center justify-center rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-amber))] text-xs font-semibold text-black">
                      今
                    </div>
                    <div className="flex flex-col gap-1">
                      <span className="text-sm font-semibold text-[color:var(--text-primary)]">
                        看今日指引 / 运势
                      </span>
                      <span className="text-xs text-[color:var(--text-secondary)]">
                        基于你的出生信息与历史模式，生成今天的能量与焦点提示。
                      </span>
                    </div>
                  </button>
                  <button
                    type="button"
                    onClick={() => setActiveSheet("mood")}
                    className="group flex items-start gap-3 rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--card-bg)] px-5 py-4 text-left shadow-[var(--card-shadow)] transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[var(--card-hover-shadow)]"
                  >
                    <div className="mt-0.5 flex h-9 w-9 items-center justify-center rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-emerald),_var(--accent-violet))] text-xs font-semibold text-white">
                      心
                    </div>
                    <div className="flex flex-col gap-1">
                      <span className="text-sm font-semibold text-[color:var(--text-primary)]">
                        快速记一笔当下心情
                      </span>
                      <span className="text-xs text-[color:var(--text-secondary)]">
                        用一句话或一个关键词，记录此刻的能量与状态，形成未来的时间纹理。
                      </span>
                    </div>
                  </button>
                  <button
                    type="button"
                    onClick={() => setActiveSheet("timeline")}
                    className="group flex items-start gap-3 rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--card-bg)] px-5 py-4 text-left shadow-[var(--card-shadow)] transition-all duration-300 hover:-translate-y-0.5 hover:shadow-[var(--card-hover-shadow)]"
                  >
                    <div className="mt-0.5 flex h-9 w-9 items-center justify-center rounded-full bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-violet))] text-xs font-semibold text-white">
                      轨
                    </div>
                    <div className="flex flex-col gap-1">
                      <span className="text-sm font-semibold text-[color:var(--text-primary)]">
                        查看时间线与模式
                      </span>
                      <span className="text-xs text-[color:var(--text-secondary)]">
                        回看你过往的梦境、指引与记录，观察螺旋上升的节奏和重复出现的主题。
                      </span>
                    </div>
                  </button>
                </div>
                <div className="mt-8 flex flex-col gap-3 text-xs text-[color:var(--text-tertiary)] md:flex-row md:items-center md:justify-between">
                  <div>
                    <span className="font-medium text-[color:var(--accent-gold)]">
                      行动 → 洞察 → 轨迹
                    </span>{" "}
                    · 每一个动作都会进入你的时间线与 Archive。
                  </div>
                  <div className="flex flex-wrap gap-2">
                    <span className="rounded-full border border-[var(--card-border)] px-3 py-1">
                      呼吸节奏 · 4–7–8
                    </span>
                    <span className="rounded-full border border-[var(--card-border)] px-3 py-1">
                      神圣几何 · 内在轨迹
                    </span>
                  </div>
                </div>
              </section>
            )}
            {view === "timeline" && (
              <section className="rounded-[var(--radius-xl)] border border-[var(--card-border)] bg-[var(--card-bg)] p-6 shadow-[var(--card-shadow)] backdrop-blur-2xl md:p-10">
                <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                  Timeline · 时间界
                </p>
                <h1 className="mt-4 text-2xl md:text-3xl">
                  时间的螺旋正在搭建中
                </h1>
                <p className="mt-3 max-w-2xl text-sm text-[color:var(--text-secondary)] md:text-base">
                  这里未来会承载你的梦境、指引与记录的时间轴，以螺旋方式呈现周期与重复主题。目前为占位视图，先让 Now 当下界承担主要行动。
                </p>
              </section>
            )}
            {view === "archive" && (
              <section className="rounded-[var(--radius-xl)] border border-[var(--card-border)] bg-[var(--card-bg)] p-6 shadow-[var(--card-shadow)] backdrop-blur-2xl md:p-10">
                <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                  Archive · 永恒界
                </p>
                <h1 className="mt-4 text-2xl md:text-3xl">
                  你的内在档案库
                </h1>
                <p className="mt-3 max-w-2xl text-sm text-[color:var(--text-secondary)] md:text-base">
                  未来这里会成为你的梦境日记、洞察卡片与成长轨迹的归档空间。当前阶段先以 Now 当下界的行动为入口，后续我们再把历史沉淀与模式识别接入。
                </p>
              </section>
            )}
          </main>
          {activeSheet !== "none" && (
            <div className="fixed inset-0 z-40 flex items-center justify-center bg-black/40 px-4">
              <div className="relative w-full max-w-lg rounded-[var(--radius-xl)] border border-[var(--card-border)] bg-[color:var(--card-bg)]/95 p-6 shadow-[var(--card-hover-shadow)] backdrop-blur-xl">
                <button
                  type="button"
                  className="absolute right-4 top-4 text-xs text-[color:var(--text-secondary)] hover:text-[color:var(--text-primary)]"
                  onClick={() => setActiveSheet("none")}
                >
                  关闭
                </button>
                {activeSheet === "dream" && (
                  <div className="space-y-4">
                    <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                      Dream · 梦境记录
                    </p>
                    <h2 className="text-xl md:text-2xl">记录刚刚的梦</h2>
                    <p className="text-sm text-[color:var(--text-secondary)]">
                      趁记忆尚清晰，把画面、情绪和关键符号写下来。未来引擎会在这里为你提炼象征与线索。
                    </p>
                    <div className="space-y-3">
                      <div className="space-y-1">
                        <label className="text-xs text-[color:var(--text-tertiary)]">给这个梦起一个名字</label>
                        <input
                          className="w-full rounded-[var(--radius-md)] border border-[var(--card-border)] bg-transparent px-3 py-2 text-sm outline-none focus:border-[var(--accent-gold)]"
                          placeholder="例如：在陌生城市追逐光"
                        />
                      </div>
                      <div className="space-y-1">
                        <label className="text-xs text-[color:var(--text-tertiary)]">梦的内容</label>
                        <textarea
                          className="h-32 w-full resize-none rounded-[var(--radius-md)] border border-[var(--card-border)] bg-transparent px-3 py-2 text-sm outline-none focus:border-[var(--accent-gold)]"
                          placeholder="自由写下你记得的一切片段、情绪和象征……"
                        />
                      </div>
                    </div>
                    <div className="flex justify-end gap-3 pt-2 text-xs text-[color:var(--text-tertiary)]">
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] border border-[var(--card-border)] px-3 py-1.5"
                        onClick={() => setActiveSheet("none")}
                      >
                        先不记了
                      </button>
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] bg-[radial-gradient(circle_at_30%_30%,_var(--accent-violet),_var(--accent-emerald))] px-4 py-1.5 text-xs font-semibold text-white"
                      >
                        临时保存这个梦
                      </button>
                    </div>
                  </div>
                )}
                {activeSheet === "guidance" && (
                  <div className="space-y-4">
                    <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                      Guidance · 今日指引
                    </p>
                    <h2 className="text-xl md:text-2xl">为今天对齐一个小小的焦点</h2>
                    <p className="text-sm text-[color:var(--text-secondary)]">
                      这里未来会基于你的出生信息和历史模式生成每日能量报告。当前阶段先给你一个简单的对齐仪式。
                    </p>
                    <div className="mt-2 grid gap-3 text-xs text-[color:var(--text-secondary)]">
                      <div className="rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--bg-middle)]/60 px-3 py-2">
                        <div className="text-[0.7rem] tracking-[0.2em] text-[color:var(--text-tertiary)]">
                          今日主调
                        </div>
                        <div className="mt-1 text-sm text-[color:var(--text-primary)]">
                          选择一件最小但真正重要的事，把注意力温柔地放在它上面。
                        </div>
                      </div>
                      <div className="rounded-[var(--radius-lg)] border border-[var(--card-border)] bg-[var(--bg-middle)]/60 px-3 py-2">
                        <div className="text-[0.7rem] tracking-[0.2em] text-[color:var(--text-tertiary)]">
                          呼吸建议
                        </div>
                        <div className="mt-1 text-sm text-[color:var(--text-primary)]">
                          用 4–7–8 的节奏，做三轮深呼吸，再开始今天的行动。
                        </div>
                      </div>
                    </div>
                    <div className="flex justify-end pt-2 text-xs text-[color:var(--text-tertiary)]">
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] border border-[var(--card-border)] px-4 py-1.5"
                        onClick={() => setActiveSheet("none")}
                      >
                        收下今日提示
                      </button>
                    </div>
                  </div>
                )}
                {activeSheet === "mood" && (
                  <div className="space-y-4">
                    <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                      Mood · 此刻心情
                    </p>
                    <h2 className="text-xl md:text-2xl">用一句话记录现在</h2>
                    <p className="text-sm text-[color:var(--text-secondary)]">
                      不需要长篇大论，一个词、一句话、一个画面都可以，关键是诚实地对齐此刻。
                    </p>
                    <div className="space-y-3">
                      <textarea
                        className="h-24 w-full resize-none rounded-[var(--radius-md)] border border-[var(--card-border)] bg-transparent px-3 py-2 text-sm outline-none focus:border-[var(--accent-emerald)]"
                        placeholder="此刻的我像……"
                      />
                      <div className="flex items-center justify-between text-xs text-[color:var(--text-tertiary)]">
                        <span>这些记录未来会在时间线中形成你的情绪纹理。</span>
                      </div>
                    </div>
                    <div className="flex justify-end gap-3 pt-2 text-xs text-[color:var(--text-tertiary)]">
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] border border-[var(--card-border)] px-3 py-1.5"
                        onClick={() => setActiveSheet("none")}
                      >
                        稍后再写
                      </button>
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] bg-[radial-gradient(circle_at_30%_30%,_var(--accent-emerald),_var(--accent-violet))] px-4 py-1.5 text-xs font-semibold text-white"
                      >
                        在心里按下保存
                      </button>
                    </div>
                  </div>
                )}
                {activeSheet === "timeline" && (
                  <div className="space-y-4">
                    <p className="text-xs font-medium uppercase tracking-[0.25em] text-[color:var(--text-tertiary)]">
                      Timeline · 时间界
                    </p>
                    <h2 className="text-xl md:text-2xl">时间的螺旋正在搭建中</h2>
                    <p className="text-sm text-[color:var(--text-secondary)]">
                      这里未来会承载你的梦境、指引与记录的时间轴，以螺旋方式呈现周期与重复主题。目前为占位视图，先让 Now 当下界承担主要行动。
                    </p>
                    <div className="flex justify-end pt-2 text-xs text-[color:var(--text-tertiary)]">
                      <button
                        type="button"
                        className="rounded-[var(--radius-md)] bg-[radial-gradient(circle_at_30%_30%,_var(--accent-gold),_var(--accent-violet))] px-4 py-1.5 text-xs font-semibold text-white"
                        onClick={() => {
                          setActiveSheet("none");
                          setView("timeline");
                        }}
                      >
                        进入时间线
                      </button>
                    </div>
                  </div>
                )}
              </div>
            </div>
          )}
          <nav className="mt-6 flex items-center justify-around border-t border-[var(--card-border)] bg-[var(--bg-middle)] py-3 text-xs text-[color:var(--text-secondary)] md:hidden">
            <button
              type="button"
              onClick={() => setView("now")}
              className={`flex flex-1 flex-col items-center gap-1 ${view === "now" ? "text-[color:var(--text-primary)]" : ""}`}
            >
              <span
                className="h-1 w-6 rounded-full"
                style={{ background: view === "now" ? "var(--accent-gold)" : "var(--card-border)" }}
              />
              <span>Now</span>
            </button>
            <button
              type="button"
              onClick={() => setView("timeline")}
              className={`flex flex-1 flex-col items-center gap-1 ${view === "timeline" ? "text-[color:var(--text-primary)]" : ""}`}
            >
              <span
                className="h-1 w-6 rounded-full"
                style={{ background: view === "timeline" ? "var(--accent-gold)" : "var(--card-border)" }}
              />
              <span>Timeline</span>
            </button>
            <button
              type="button"
              onClick={() => setView("archive")}
              className={`flex flex-1 flex-col items-center gap-1 ${view === "archive" ? "text-[color:var(--text-primary)]" : ""}`}
            >
              <span
                className="h-1 w-6 rounded-full"
                style={{ background: view === "archive" ? "var(--accent-gold)" : "var(--card-border)" }}
              />
              <span>Archive</span>
            </button>
          </nav>
          <footer className="mt-6 flex flex-col justify-between gap-2 text-xs text-[color:var(--text-tertiary)] md:flex-row">
            <span>LucidSelf · 内在成长实验空间</span>
            <span>初始版本 · 前端仍在缓慢呼吸与成形中</span>
          </footer>
        </div>
      </div>
    </div>
  );
}

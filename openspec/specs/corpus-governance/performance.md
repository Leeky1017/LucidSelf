# 语料治理 — 性能目标与测量方式（规格层）

本文件定义 `corpus-governance` 的性能目标与最小测量口径。禁止写实现细节；仅定义 MUST/SHALL 与可判定口径。

## 1) 性能目标（最小）

- 许可校验与血缘解析 SHOULD 在可接受窗口内完成（阈值由运行方定义但必须可审计）。
- 资产登记与退役操作 SHOULD 在 1 分钟内可见（用于下游阻断与引用追溯）。

## 2) 测量口径（最小）

- 关键操作 MUST 记录开始/结束时间与结果，并可按 `asset_id/corpus_release_id` 定位（在不违反最小化原则下）。


# 语料治理 — 成本目标与预算阈值（规格层）

本文件定义 `corpus-governance` 的成本目标、预算阈值与告警策略。禁止写实现细节；仅定义 MUST/SHALL 与可判定口径。

## 1) 成本维度（最小）

- **存储**：资产登记、许可、血缘、退役记录与审计导出
- **计算**：许可校验与血缘解析

## 2) 预算阈值（最小）

- 许可违规与血缘不可解析事件异常抬升 MUST 告警并阻断发布（资产门禁口径）。

## 3) 告警策略（最小）

- 超过阈值（连续 3 个评估窗口）MUST 告警，并形成可审计事件。

